grist:
  schemas:
    tables:
      services:
        id: Services_numeriques
        columns:
          - id: identifiant
            fields:
              label: Identifiant
          - id: name
            fields:
              label: Nom
          - id: mission
            fields:
              label: Mission
          - id: link
            fields:
              label: Lien
          - id: impact_url
            fields:
              label: Matrice d'impact
          - id: current_phase
            fields:
              label: Phase
          - id: contact_email
            fields:
              label: Email de contact
      members:
        id: Membres_beta_lecture_seule
        columns:
          - id: identifiant
            fields:
              label: Identifiant
          - id: name
            fields:
              label: Nom
          - id: gender
            fields:
              label: Genre
          - id: email
            fields:
              label: Email
          - id: secondary_email
            fields:
              label: Email secondaire
          - id: domain
            fields:
              label: Domaine
          - id: active_mission
            fields:
              type: Bool
              label: Actif
